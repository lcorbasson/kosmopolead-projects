<%= render :partial => 'list_translations'%>
<!-- Ajout d'un identifier et d'une traduction -->
<% remote_form_for(:activity_sector, @activity_sector,
                 :url => {:controller => 'activity_sectors', :action => 'create'},
                 :method => :post,
                 :html => {:class=>"tabular",:id => 'new-activity_sector-form', :style => (@relation ? '' : 'display: none;')}) do |f| %>
<%= box(
    "#{l(:label_new_activity_sector)}",
    "#{render:partial=>'activity_sectors/new', :locals => {:f => f}}"
) %>

<% end %>


