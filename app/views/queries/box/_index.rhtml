<% if queries.empty? %>
  <p><i><%=l(:label_no_data)%></i></p>
<% else %>
  <table class="list">
  <% queries.each do |query| %>
    <tr class="<%= cycle('odd', 'even') %>">
      <td>
        <%  if query.query_type=="issue" %>
          <%= link_to_remote query.name, {:url=>{:controller => 'issues', :action => 'index', :project_id => @project, :query_id => query}} %>
        <% else %>
          <%= link_to_remote query.name, {:url=>{:controller => 'queries', :action => 'projects',  :query_id => query}} %>
        <% end %>
      </td>
      <td align="right">
        <small>
        <% if query.editable_by?(User.current) %>
        <%= link_to image_tag("edit.png"), {:controller => 'queries', :action => 'edit', :id => query}, :class => 'icon' %>
        <%= link_to image_tag("delete.png"),  query_path(query),:method=>:delete,:confirm=>"Etes-vous certain de vouloir supprimer cette requête ?" , :class => 'icon' %>
        </small>
      <% end %>
      </td>
    </tr>
  <% end %>
  </table>
<% end %>



